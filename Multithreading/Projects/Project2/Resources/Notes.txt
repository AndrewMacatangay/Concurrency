Basic Info (Just ticker):
shortName fullExchangeName/quoteSourceName regularMarketPrice
marketCap
circulatingSupply (crypto)

Today:
regularMarketPrice regularMarketChange regularMarketChangePercent
regularMarketLow regularMarketHigh
regularMarketPreviousClose regularMarketOpen
bid ask (non-crypto)

DayAverage:

Volume:

Year:

Finish buy/sell. Use unordered_map instead of vector?

